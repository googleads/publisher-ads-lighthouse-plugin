{
  "compilerOptions": {
    "noEmit": true,
    "module": "CommonJS",
    "target": "ES2017",
    "allowJs": true,
    "checkJs": true,
    "strict": true,
    // TODO: set noImplicitAny to true after errors in lighthouse-core are
    // resolved. This would require all variables to have known types.
    "noImplicitAny": false,
    "typeRoots": [
      "./node_modules/@types",
      "./typings"
    ]
  },
  "include": [
    "**/*.js",
    "**/*.d.ts",
  ],
  "exclude": [
    "test/**/*.js"
  ]
}
